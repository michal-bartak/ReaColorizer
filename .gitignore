# macOS
.DS_Store

# REAPER writes the rule set outside this folder, but just in case
config.json
config.bak.json
config.bad.json

# test scratch
tests/.tmp/

# Claude Code session data and worktrees (248M of node_modules in here)
.claude/
